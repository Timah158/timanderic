type ProjectSection @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String
  images: [AWSURL]
  text: [String!]!
  links: [AWSURL]
  index: Int!
  projectID: ID! @index(name: "byProject")
}

type Project @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String!
  createdOn: AWSDate!
  completedOn: AWSDate
  description: String!
  createdBy: String!
  hidden: Boolean!
  status: String
  ProjectSections: [ProjectSection] @hasMany(indexName: "byProject", fields: ["id"])
}

type Interview @model @auth(rules: [{allow: public}]) {
  id: ID!
  email: AWSEmail!
  phone: AWSPhone!
  date: AWSDate!
  time: AWSTime!
  about: String
  user: String!
}
 